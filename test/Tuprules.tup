REQUIRED_ARCHIVES += $(ROOT)/third-party/libtap/tap.a
REQUIRED_ARCHIVES += $(ROOT)/src/apoapsis.a
REQUIRED_ARCHIVES += $(TUP_CWD)/TestCore.o

CFLAGS += $(INTERNAL_CFLAGS)
LDFLAGS += $(INTERNAL_LDFLAGS)
LDFLAGS = $(REQUIRED_ARCHIVES) $(LDFLAGS)
