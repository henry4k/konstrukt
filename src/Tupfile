include_rules

: foreach *.cpp |> !cxx |> %B.o {objects}

: |> ^o Generating cflags file^ echo $(CFLAGS) $(CXXFLAGS) > cflags |> cflags

LDFLAGS += $(INI_A) $(SQUIRREL_A) $(SQSTDLIB_A)
: {objects} | $(INI_A) $(SQUIRREL_A) $(SQSTDLIB_A) |> !ldcxx |> apoapsis
