include_rules

CFLAGS += $(INTERNAL_CFLAGS)
LDFLAGS += $(INTERNAL_LDFLAGS)

: foreach *.cpp |> !cxx |> %B.o {objects}
: {objects} ControlBindings/*.o LuaBindings/*.o |> !ar |> apoapsis.a

: apoapsis.a $(REQUIRED_ARCHIVES) |> !ldcxx |> apoapsis
