include_rules

: foreach *.cpp |> !cxx |> %B.o {objects}

: |> ^o Generating cflags file^ echo $(CFLAGS) $(CXXFLAGS) > cflags |> cflags

LDFLAGS += $(INI_A) $(SQUIRREL) $(SQSTDLIB)
: {objects} | $(INI_A) $(SQUIRREL) $(SQSTDLIB) |> !ldcxx |> apoapsis
