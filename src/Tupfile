include_rules

CFLAGS += $(INTERNAL_CFLAGS)
LDFLAGS += $(INTERNAL_LDFLAGS)

: foreach *.cpp |> !cxx |> %B.o {objects}
: {objects} |> !ar |> apoapsis.a

: $(REQUIRED_ARCHIVES) apoapsis.a |> !ldcxx |> apoapsis
