option(APOAPSIS_BUILD_TESTS "Build native tests")
if(APOAPSIS_BUILD_TESTS)
    macro(AddTest TestName)
        add_executable(${TestName}.t ${TestName}.cpp TestTools.cpp)
        target_link_libraries(${TestName}.t apoapsislib dummy)
    endmacro()
    AddTest(Common)
    AddTest(Math)
endif()
